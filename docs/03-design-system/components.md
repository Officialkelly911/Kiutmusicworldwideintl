# UI Components

Catalogues recurring UI patterns — cards, badges, buttons, dividers, and pill labels — with their shared border radius (`rounded-2xl`/`rounded-3xl`), shadow, and border-opacity conventions.
