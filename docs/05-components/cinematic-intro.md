# CinematicIntro

Documents the branded loading screen shown on first homepage visit, featuring the K monogram, animated gold waveform bars, "Loading Experience" text, and the `sessionStorage` flag that prevents repeat displays.
