# PlayerContext

Documents the global floating mini-player context (`client/src/context/PlayerContext.tsx`), its `usePlayer` hook API, and the state fields: `currentTrack`, `isPlaying`, `currentTime`, `duration`, `seek`, and `playTrack`.
