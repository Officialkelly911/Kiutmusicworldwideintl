# Floating Music Player

Documents the persistent mini-player feature — its artwork, title, play/pause, progress bar, and next-track controls — and explains how it reads from and writes to `PlayerContext` without obstructing page content.
