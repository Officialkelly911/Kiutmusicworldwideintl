# Environment Variables

Documents any environment variables or secrets required by the project and explains how they are managed through Replit's built-in secrets system rather than committed `.env` files.
